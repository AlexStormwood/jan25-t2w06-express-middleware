info:
  name: Create User
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/users/register
  body:
    type: json
    data: |-
      {
        "email":"test@email.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
